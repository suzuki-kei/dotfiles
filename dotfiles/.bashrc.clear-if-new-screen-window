#
# GNU Screen でウィンドウを作成したときに clear コマンドを実行する.
#
# 使用方法:
#     ~/.bashrc の一番最後で本スクリプトを source で読み込む.
#
# 背景:
#     .bashrc の読み込み完了前に入力すると "cd$ cd" のように表示が乱れる.
#     GNU Screen 使用中は頻繁にウィンドウの作成/破棄をおこなうので表示の乱れに遭遇する機会が多い.
#     そのため, GNU Screen 使用中のみ .bashrc の最後に clear コマンドを実行する.
#
if [[ "${TERM}" == 'screen' ]]; then
    clear
fi

