# dotfiles

## Install (for Linux)

    git clone git@github.com:suzuki-kei/dotfiles.git
    cd dotfiles
    ln -sf "$(pwd)/.bashrc" "~/.bashrc"

## Install (for Mac)

    git clone git@github.com:suzuki-kei/dotfiles.git
    cd dotfiles
    ln -sf "$(pwd)/.bashrc-mac" "~/.bashrc"

